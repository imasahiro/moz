#include <stdio.h>

// Choice();
// Sequence();
// Option();
// Byte();
// Str();
// Set();
// Repetition();
// Repetition1();
// Any();

typedef struct dfa {
} dfa_t;

typedef struct nfa {
} nfa_t;

dfa_t *nfa2dfa(nfa_t *nfa);
